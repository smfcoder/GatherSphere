Navigate into the Frontend directory. Initialize a new Next.js project using the App Router configured to run on port 3000. Install next-auth and configure the boilerplate for a Keycloak OIDC provider. Create the directory structure and pages for a public Home page, a protected User dashboard, and a protected Admin dashboard. Implement logic to read the user roles from the Keycloak session token to conditionally render navigation links and protect the Admin dashboard. Create a utility demonstrating how to send an authenticated fetch request with a Bearer token to the FastAPI backend at http://localhost:8000